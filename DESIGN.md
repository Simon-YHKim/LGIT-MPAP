# LG Innotek 설비 생산성 분석 플랫폼 — DESIGN.md

> **Direction**: 🅐 Calm Engineering (Linear · Stripe · Toss 영향, 도구형 톤)
> **Generated by**: `simon-design-first` (Step 1-4) → `stitch-design-flow` (입력 사양)
> **Audience**: LG Innotek 광학 사업부 사내 엔지니어 (한국어, 데스크톱)

---

## 1. Product

| 항목 | 값 |
|---|---|
| 이름 | 설비 생산성 분석 플랫폼 (Equipment Productivity Analytics Platform, 약칭 **VITALS**) |
| 한 줄 피치 | LG Innotek 광학 사업부의 설비 생산성을 CMP · UPH · MTBA 세 축으로 통합 분석·조회하는 사내 엔지니어링 대시보드 |
| 소속 | 광학 Max Capa TDR |
| 스택 | Streamlit (Python) · PostgreSQL · Plotly · st-aggrid |

## 2. Audience

- **1차**: LG Innotek 광학 사업부 현장/공정 엔지니어 (한국어 모국어, 사내망 PC 1080p~1440p 데스크톱)
- **2차**: 분석 담당, 생산기술/품질, 라인 매니저
- **빈도**: 매일~주간 수치 확인, 이상 발생 시 드릴다운
- **환경**: 사내 인트라넷, 마우스+키보드, 데스크톱 전용 (모바일 비대상)

## 3. Tone keywords

`engineering`, `legible`, `dense`, `calm`, `tool-like`

> "꾸미지 않은 정확함" — 장식 < 정보 밀도. 도구 같은 인상.
> Linear 가 LG 사옥에 들어가면 이렇게 생겼을 듯한 톤.

## 4. Color palette (3색 정책)

본문은 거의 무채. **와인레드는 강조점 1~2개**에만, **Status 3색**은 데이터 의미용.

| Role | Hex | Usage |
|---|---|---|
| **Primary — Wine Red** | `#A50034` | 페이지 좌측 1px 마커 / 활성 탭 밑줄 / Primary CTA / KPI 변화량 강조 / sticky 컬럼 right-border |
| **Primary Dark** | `#7E0027` | hover, focus ring 어두운 톤 |
| **Primary Tint** | `#F8E5EC` | active row 배경, badge 배경 |
| **Surface — Page** | `#F7F8FA` | 전체 페이지 배경 (살짝 따뜻한 회백) |
| **Surface — Card** | `#FFFFFF` | 카드, 표, 필터 박스 |
| **Surface — Soft** | `#F1F3F5` | 표 thead, hover row, disabled |
| **Border** | `#E5E7EB` | 1px 라인 (카드, 표 셀 구분) |
| **Border Strong** | `#CBD0D6` | 강한 구분 (sticky 컬럼 right) |
| **Ink — Body** | `#1F2430` | 본문 (순흑 금지, 살짝 violet tint) |
| **Ink — Muted** | `#6B7280` | 부제, 캡션, placeholder |
| **Ink — Subtle** | `#9CA3AF` | 비활성 라벨 |
| **Status — Good** | `#1F8B4C` | 임계 통과/정상 |
| **Status — Warn** | `#B57F1B` | 주의 |
| **Status — Bad** | `#B23A48` | 임계 초과/이상 (와인레드와 다른 톤) |

### Anti-rules

- ❌ pure black (`#000`) / pure gray — 살짝 violet/blue tint 필수
- ❌ 4색 이상 multi-color 차트 (monotone 와인레드 그라디언트 또는 Status 3색만)
- ❌ 페이지 전체 그라디언트 배경
- ❌ 채도 높은 보라/파랑/시안

## 5. Typography

| Role | Font | Weights | Usage |
|---|---|---|---|
| **Body / UI / Label (1순위)** | `LG Smart` (LG 코퍼레이트) | 300, 400, 600, 700 | 사내 PC 에 설치된 LG 폰트를 `local()` 로 우선 매칭 (`LG Smart UI` / `LG Smart_H` / `LGSmHaTR` / `LG EI Text` 등 다양한 설치 변형 지원) |
| **Body / UI / Label (Fallback)** | `Pretendard Variable` | 400, 600, 700 | LG 폰트 미설치 환경 대체. CDN: `https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/variable/pretendardvariable.css` |
| **Numbers / KPI / Table cells** | `IBM Plex Mono` | 400, 600 | KPI 숫자, 표 숫자 셀 |
| **System Fallback** | `'Malgun Gothic', system-ui, sans-serif` | — | 모든 외부 CDN 차단 시 |

**Font stack** (CSS): `'LG Smart', 'Pretendard Variable', Pretendard, 'Malgun Gothic', system-ui, sans-serif`

**원리**: 사내 PC 에서는 LG 코퍼레이트 폰트로 자연스레 렌더, 외부 환경(갤탭/모바일/외부망)에서는 Pretendard 로 graceful fallback.

### Type scale (desktop)

| Token | px | line-height | weight | use |
|---|---|---|---|---|
| `display` | 28 | 1.2 | 700 | 페이지 H1 |
| `heading-lg` | 18 | 1.3 | 700 | 섹션 헤딩 |
| `heading-md` | 14 | 1.4 | 700 | 카드/필터 타이틀 |
| `body` | 14 | 1.5 | 400 | 기본 본문 |
| `body-md` | 13 | 1.5 | 400 | 표 셀, 라벨, 사이드바 |
| `body-sm` | 12 | 1.5 | 400 | 캡션 |
| `label-xs` | 11 | 1.4 | 700 (uppercase) | eyebrow (`PRODUCTIVITY`) |
| `kpi-xl` | 32 | 1.1 | 700 (Plex Mono) | KPI 큰 숫자 |
| `kpi-md` | 18 | 1.2 | 600 (Plex Mono) | 표 숫자 |

### 한국어 룰

- `word-break: keep-all`
- `letter-spacing: -0.01em` (heading), `0` (body)

## 6. Layout & density

| 항목 | 값 |
|---|---|
| 데스크톱 baseline | 1280px ~ 1880px |
| 콘텐츠 max-width | **1880px** |
| 사이드바 폭 | **240px** (현재 300px → 60px 절감) |
| Card radius | **8px** |
| Card padding | 16px (작은 카드 12px·16px) |
| 표 행 높이 | **32px** (sticky thead 36px), 셀 padding `6px 10px` |
| KPI 카드 | **88px 고정**, 라벨 + 숫자 + delta 3행 |
| 모달 / 드릴인 | 우측 슬라이드인, 폭 **480~640px** |

## 7. Component primitives (9종)

1. **App Shell** — 좌측 240px 고정 사이드바 + 콘텐츠. 상단 strip은 1px 와인레드 hairline (또는 없음, 변형에 따라)
2. **Page Header** — 11px uppercase eyebrow + 28px H1 + 13px sub + 우측 toolbar(Refresh / Export / Range)
3. **Filter Bar** — 가로 1줄, 라벨 위·인풋 아래, max 5 inputs, 우측 끝 Apply / Reset
4. **KPI Card** — 11px label + 32px Plex Mono 숫자 + delta(±%, status color) + 옵션 sparkline
5. **Section heading** — 좌측 4px 와인레드 vertical bar + 18px 700 + 우측 secondary action
6. **Data Table** — sticky thead, sticky 좌측 1~3 컬럼, 행 hover Surface Soft, active row 좌측 2px 와인 마커, 숫자 우측정렬·텍스트 좌측정렬
7. **Chart card** — Plotly. monotone 와인 그라디언트 또는 Status 3색만. 격자 `#EFF1F4`. tick label 12px
8. **Drill-down panel** — 표 셀 클릭 → 우측 슬라이드인 패널. 탭(요약 / 상세 / 히스토리), close X
9. **Empty / Loading / Error** — 각 카드 3 상태 stub 필수. 차분한 한국어 카피

## 8. Key screens

`stitch-design-flow`가 이 4개에 대해 A/B/C 변형을 만든다:

### S1. Login
- 1열 풀스크린, 우측 카드 폼만 (좌측 비주얼 영역은 절제 — 단색 + 작은 LG Innotek 마크)
- 탭: 로그인 / 회원가입
- 인풋: 사내 ID(자동 `@lginnotek.com`) + 비밀번호 / 회원가입은 회사 이메일 + 인증코드
- 사이드바·헤더 노출 X

### S2. Home (분석 항목 선택)
- 페이지 헤더(eyebrow `PLATFORM` + H1 "분석 도구")
- 5개 분석 도구 카드 그리드 (CMP / UPH / MTBA Dashboard / MTBA Detail / MaxCapa Chat)
- 각 카드: 라인 아이콘 + 타이틀 + 한 줄 설명 + 상태 뱃지(`정식` / `가오픈` / `오픈예정`)
- 푸터: "제작: 광학 Max Capa TDR"

### S3. CMP Dashboard (대표 대시보드)
- 페이지 헤더(eyebrow `PRODUCTIVITY · CMP` + H1 "CMP 달성률 Dashboard" + sub `'26.4월 4W 기준`)
- 필터 바 1줄: 영역 multi · 모델 multi · 기간 range · [Reset] [Apply]
- KPI row 4종 (전체 / R50 / R53A / R53B 달성률) + delta
- 섹션: 모델별 박스 그리드 (공정별 달성률 모델 박스)
- 섹션: 요약 표 (sticky)
- 섹션: 공정 선택 → 하단에 CMP / UPH / Efficiency 상세 표/차트

### S4. MTBA Detail View (드릴다운 화면)
- 페이지 헤더 + 우측 [페이지 상태 초기화] [+ 패널 추가]
- 모델 선택 단일 라인
- 패널 카드 (`soft-card`): 조회 토글 + info chip 행 + 표(st-aggrid)
- 표 셀 클릭 → 우측 슬라이드인 디테일 패널 (요약 / 상세 / 메모·이미지 탭)
- 디버그 정보 collapsible

## 9. Reference products

| Borrow from | What |
|---|---|
| **Linear** (linear.app) | 정보 계층, 키보드 친화, 무채 톤 |
| **Stripe Dashboard** (stripe.com/dashboard) | 표 가독성, KPI 카드, 필터 바 |
| **Toss** (toss.im) | Pretendard 운용, 한국어 여백 |
| **LG Innotek** (lginnotek.com) | 와인레드 강도 보정 (악센트 1점) |

### Avoid

- ❌ 어드민 대시보드 부트스트랩 디폴트 (Tabler, AdminLTE)
- ❌ SaaS 마켓 템플릿의 4-5색 multi-color 차트
- ❌ 현재 페이지의 "버건디 그라디언트 헤더 + 22px 라운드" — 너무 장식적
- ❌ 이모지 아이콘
- ❌ 큰 hero gradient

## 10. Constraints (모든 시안 공통)

- 데스크톱 우선 1280px ~ 1880px
- WCAG AA (텍스트 4.5:1, UI 3:1)
- 한국어 1순위 + 영문 라벨 병기 (`MTBA(Mean Time Between Alarm)`)
- 라인 아이콘만 (Lucide / Phosphor) — 이모지 X
- 배경 이미지 / 스톡 사진 X
- 폰트 weight max 3개 (400 / 600 / 700)
- 3색 정책 (무채 본문 + 와인레드 강조 + Status 3색)
- v1: 라이트 모드만 (다크는 v2)
- easing: `cubic-bezier(0.2, 0.6, 0.3, 1)` (bounce/elastic 금지)

## 11. Out-of-scope (v1)

- 다크 모드
- 모바일 레이아웃
- 풀 i18n (영문 UI)
- 페이지 트랜지션·스켈레톤 외 모션
